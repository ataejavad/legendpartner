{
  "name": "legend-relationship-identity",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dating & Relationship Public Profile — profile, relationship verification, proposals, discovery, trust.",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node --no-warnings server.js",
    "seed": "node --no-warnings seed.js",
    "test": "node --no-warnings test/e2e.js"
  }
}
